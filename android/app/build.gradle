plugins { id 'com.android.application' }
android {
 namespace 'art.gemosdodo.still'
 compileSdk 35
 defaultConfig { applicationId 'art.gemosdodo.still'; minSdk 28; targetSdk 35; versionCode 4; versionName '0.4-lite-experimental'; ndk { abiFilters 'arm64-v8a' } }
 signingConfigs { debug { storeFile file(System.getProperty("user.home") + "/.android/gemos-debug.keystore"); storePassword "android"; keyAlias "androiddebugkey"; keyPassword "android" } }
 compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
 packaging { jniLibs { useLegacyPackaging = false } }
}
dependencies { implementation 'com.microsoft.onnxruntime:onnxruntime-android:1.24.2' }
